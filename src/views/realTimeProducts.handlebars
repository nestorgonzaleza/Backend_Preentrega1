<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Productos en tiempo real</title>
    <script src="/socket.io/socket.io.js"></script>
</head>
<body>
    <div>

    <h1>Lista de productos en tiempo real</h1>

    {{#each productos}}
    <div>
        <p>{{this.title}}</p>
        <p>{{this.price}}</p>
    </div>
    {{/each}}
    </div>

    <!-- Llama a tu archivo JavaScript después de que se haya cargado el cliente de Socket.IO -->
    <script src="/realtimeproducts.router.js"></script>
</body>
</html>